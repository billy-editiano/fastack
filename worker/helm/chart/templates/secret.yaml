{{- if and (not .Values.database.password.existingSecret) (not .Values.database.password.value) }}
{{- fail "database.password.value or database.password.existingSecret must be set" }}
{{- end }}
{{- if and (not .Values.database.password.existingSecret) .Values.database.password.value }}
# Only create a secret when no external secret is provided.
apiVersion: v1
kind: Secret
metadata:
  name: {{ include "chart.fullname" . }}-db
  labels:
    {{- include "chart.labels" . | nindent 4 }}
type: Opaque
stringData:
  password: {{ .Values.database.password.value | quote }}
{{- end }}
