{{- $rootSecret := .Values.auth.rootPassword.existingSecretName }}
{{- $rootValue := .Values.auth.rootPassword.value }}
{{- $passwordSecret := .Values.auth.password.existingSecretName }}
{{- $passwordValue := .Values.auth.password.value }}
{{- if and (not $rootSecret) (not $rootValue) (not $passwordSecret) (not $passwordValue) }}
{{- fail "at least one of auth.rootPassword.value, auth.rootPassword.existingSecretName, auth.password.value, or auth.password.existingSecretName must be set" }}
{{- end }}
{{- if or $rootValue $passwordValue }}
apiVersion: v1
kind: Secret
metadata:
  name: {{ include "chart.fullname" . }}-auth
  labels:
    {{- include "chart.labels" . | nindent 4 }}
type: Opaque
stringData:
  {{- if $rootValue }}
  root-password: {{ $rootValue | quote }}
  {{- end }}
  {{- if $passwordValue }}
  password: {{ $passwordValue | quote }}
  {{- end }}
{{- end }}
